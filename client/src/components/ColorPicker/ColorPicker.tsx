import styles from "./ColorPicker.module.scss";

const ColorPicker = () => {
  // TODO
  return (
    <div className={styles.container}>
      <input type="color" />
    </div>
  );
};

export default ColorPicker;
